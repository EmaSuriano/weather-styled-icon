import React from 'react';
import Section from './Section';
import palette from './palette';

const InAction = () => (
  <Section backgroundColor={palette.queenBlue}>
    <h1>In action</h1>
    <p>import sunny from 'weather-styled-icon';</p>
  </Section>
);

export default InAction;
